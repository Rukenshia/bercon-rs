/*use self::hyper::Client;
use self::hyper;*/

pub fn is_listed(guid: &str) -> bool {
    /*let client = Client::new();

    let res = client.get(&format!("http://in.fkn.space:2888/check/{}", guid).send().unwrap();

    res.status == hyper::Ok*/
    false
}